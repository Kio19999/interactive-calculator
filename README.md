# Interactive Calculator (CLI)

A command-line calculator implemented in Python with a REPL loop supporting add, subtract, multiply, and divide.

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
